DEFAULT_DISPLAY_SIZE = 750 # Defines how large an image is displayed as when first loaded by PIV2
SCALE_DELTA = 1.3 # Defines the size an image becomes when it is scaled
WRAP_AROUND = True # Defines whether PIV2 should go back to the first image when pressing next while on the last image, and vice versa 
POST_LIMIT = 10 # Defines the limit of posts to get from a Reddit search
DEFAULT_SORT = "Hot" # Defines the default sort for Reddit searches
SCALE_CAP = 1 # Defines how scaled up an image can be